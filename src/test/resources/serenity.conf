webdriver {
  driver = provided

  provided {
    type = saucemobile
    saucemobile = "com.saucedemo.driver.SauceMobileDriver"
  }
}

serenity {
  logging = "VERBOSE"
  take.screenshots = AFTER_EACH_STEP
}